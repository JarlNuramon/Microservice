
server:
  port: 8082 
spring:
  application:
    name: "config-service"
  cloud:
    config:
      server:
        git:
          uri:
          username: 
          password: 
          clone-on-start: true
